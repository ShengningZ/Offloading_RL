import grpc
import object_tracking_pb2
import object_tracking_pb2_grpc

def run():
    with grpc.insecure_channel('edge_server_address:50051') as channel:
        stub = object_tracking_pb2_grpc.ObjectDetectionServiceStub(channel)
        response = stub.ProcessFrame(object_tracking_pb2.FrameRequest(frame=frame_data))
        print("Object detection service responded with message:", response.frame)

if __name__ == '__main__':
    run()